type: install
name: Jelasticozor Consumer UI

settings:
  fields:
    - name: image
      caption: Docker image
      type: string
      required: true
      
nodes:
  - count: 1
    cloudlets: 4
    nodeGroup: bl
    nodeType: docker
    image: ${settings.image}
    env:
      COMMIT_SHORT_SHA: ${settings.shortsha}
skipNodeEmails: true
